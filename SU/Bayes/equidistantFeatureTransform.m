% TODO
% transforms X features into subinterval of same ranges
% if X is the matrix each column is transformed independently
% n is the number of buckets (subintervals)
% model is the transformer model
function [trans, model] = equidistantFeatureTransform (X, n, model)
	
end
